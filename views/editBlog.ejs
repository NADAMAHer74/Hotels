<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Admin Courses</title>

    <!-- Font Awesome -->
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css"
    />
    <!-- AdminLTE CSS -->
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/admin-lte@3.1/dist/css/adminlte.min.css"
    />
    <!-- Bootstrap 5 CSS -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
  </head>

  <body class="hold-transition sidebar-mini layout-fixed">
    <div class="wrapper">
      <!-- Navbar -->
      <nav class="main-header navbar navbar-expand navbar-white navbar-light">
        <ul class="navbar-nav">
          <li class="nav-item">
            <a class="nav-link" data-widget="pushmenu" href="#" role="button"
              ><i class="fas fa-bars"></i
            ></a>
          </li>
          <li class="nav-item d-none d-sm-inline-block">
            <a href="/" class="nav-link">Home</a>
          </li>
        </ul>
      </nav>

      <!-- Sidebar -->
      <aside class="main-sidebar sidebar-dark-primary elevation-4">
        <a href="/" class="brand-link">
          <span class="brand-text font-weight-light">AdminLTE 4</span>
        </a>
        <div class="sidebar">
          <nav class="mt-2">
            <ul
              class="nav nav-pills nav-sidebar flex-column"
              data-widget="treeview"
              role="menu"
              data-accordion="false"
            >
              <li class="nav-item">
                <a href="/api/tours" class="nav-link">
                  <i class="nav-icon fas fa-tachometer-alt"></i>
                  <p>Toures</p>
                </a>
              </li>

              <li class="nav-item">
                <a href="/api/destinations" class="nav-link">
                  <i class="nav-icon fas fa-tachometer-alt"></i>
                  <p>Distinations</p>
                </a>
              </li>
              <li class="nav-item">
                <a href="/api/contactus" class="nav-link">
                  <i class="nav-icon fas fa-tachometer-alt"></i>
                  <p>Contact Us</p>
                </a>
              </li>
              <li class="nav-item">
                <a href="/api/aboutus" class="nav-link">
                  <i class="nav-icon fas fa-tachometer-alt"></i>
                  <p>About Us</p>
                </a>
              </li>
              <li class="nav-item">
                <a href="/api/aboutusimages" class="nav-link">
                  <i class="nav-icon fas fa-tachometer-alt"></i>
                  <p>About Us Images</p>
                </a>
              </li>
              <li class="nav-item">
                <a href="/api/blogs" class="nav-link">
                  <i class="nav-icon fas fa-tachometer-alt"></i>
                  <p>Blogs</p>
                </a>
              </li>
              <li class="nav-item">
                <a href="/api/blogs/new" class="nav-link">
                  <i class="nav-icon fas fa-plus"></i>
                  <p>Add Blogs</p>
                </a>
              </li>
              <li class="nav-item">
                <a href="/api/user_tours" class="nav-link">
                  <i class="nav-icon fas fa-tachometer-alt"></i>
                  <p>Reservations</p>
                </a>
              </li>
              <li class="nav-item">
                <a href="/api/pages" class="nav-link">
                  <i class="nav-icon fas fa-tachometer-alt"></i>
                  <p>pages</p>
                </a>
              </li>
            </ul>
          </nav>
        </div>
      </aside>


      <!-- Content Wrapper -->
      <div class="content-wrapper">
        <!-- Content Header -->
        <section class="content-header">
          <div class="container-fluid">
            <div class="row mb-2">
              <div class="col-sm-6">
                <h1>All Blogs</h1>
              </div>
              <div class="col-sm-6">
                <ol class="breadcrumb float-sm-right">
                  <li class="breadcrumb-item"><a href="#">Home</a></li>
                  <li class="breadcrumb-item active">All Blogs</li>
                </ol>
              </div>
            </div>
          </div>
        </section>
        <!-- Main content -->
        <section class="content">
          <div class="container-fluid">
            <div class="card card-primary card-outline">
              <div class="card-header">
                <h3 class="card-title">All Blogs</h3>
                <a
                  href="/api/admin/blogs/new"
                  class="btn btn-primary float-right"
                  >Add New Destination</a
                >
              </div>
              <!-- Table displaying courses -->
              <div class="card card-primary card-outline mb-4">
                <div class="card-header">
                  <div class="card-title">Quick Example</div>
                </div>
                <!--begin::Form-->

                <form id="editBlogForm" enctype="multipart/form-data">
                  <div class="card-body">
                    <div class="mb-3">
                      <div class="form-group">
                        <label for="title">title</label>
                          <input
                            type="text"
                            class="form-control"
                            id="title"
                            name="title"
                            required
                            minlength="2"
                            value="<%= blog.title || '' %>"
                          />
                        </div>
                      </div>
                      <div class="mb-3">
                        <div class="form-group">
                          <label for="name">content</label>
                          <input
                            type="text"
                            class="form-control"
                            id="content"
                            name="content"
                            required
                            minlength="5"
                            value="<%= blog.content || '' %>"
                          />
                        </div>
                      </div>

                      <div class="mb-3">
                        <div class="form-group">
                          <label for="name">author_id</label>
                          <input
                            type="integer"
                            class="form-control"
                            id="author_id"
                            name="author_id"
                            required
                            minlength="1"
                            value="<%= blog.author_id || '' %>"
                          />
                        </div>
                      </div>
                      <div class="mb-3">
                        <div class="form-group">
                          <label for="overview">imageUrl</label>
                          <input
                            type="file"
                            class="form-control-file"
                            id="imageUrl"
                            name="imageUrl"
                          >
<%= blog.imageUrl || '' %></input
                          >
                        <img
                          src="/<%= blog.imageUrl || '' %>"
                          alt="blog image"
                          style="width: 150px; margin-top: 10px"
                        />
                      </div>
                    </div>

                    <div class="mb-3">
                      <button type="submit" class="btn btn-primary">
                        Update Blog
                      </button>
                    </div>
                  </div>
                </form>
                <!--end::Form-->
              </div>
            </div>
          </div>
        </section>
      </div>

      <!-- Footer -->
      <footer class="main-footer">
        <strong
          >Copyright &copy; 2014-2024
          <a href="https://adminlte.io">AdminLTE.io</a>.</strong
        >
        All rights reserved.
        <div class="float-right d-none d-sm-inline">Anything you want</div>
      </footer>
    </div>

    <!-- Scripts -->
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/admin-lte@3.1/dist/js/adminlte.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
<!--     <script>
        document.getElementById("editBlogForm").addEventListener("submit", async function (event) {
          event.preventDefault(); // Prevent the default form submission
      
          const form = document.getElementById("editBlogForm");
      
          // Create FormData object for form data, including the image file
          const formData = new FormData();
          formData.append("title", form.title.value);
          formData.append("content", form.content.value);
          formData.append("author_id", form.author_id.value);
      
          // Append the image file if one is selected
          if (form.imageUrl.files[0]) {
            formData.append("imageUrl", form.imageUrl.files[0]);
          }
      
          const blogId = "<%= blog.blog_id %>"; // Assuming `blog_id` is the identifier in your database
      
          try {
            const response = await fetch(`/api/blogs/${blogId}`, {
              method: "PUT",
              body: formData, // Send FormData directly without setting Content-Type
            });
      
            if (response.ok) {
              alert("Blog updated successfully");
              window.location.href = "/api/blogs"; // Redirect to blog list
            } else {
              const errorData = await response.json();
              const errorMessage = Array.isArray(errorData)
                ? errorData.join(", ")
                : errorData.message || JSON.stringify(errorData);
              alert("Failed to update blog: " + errorMessage);
            }
          } catch (error) {
            console.error("Error:", error);
            alert("An error occurred while updating the blog");
          }
        });
      </script> -->
      <script>
        document.getElementById("editBlogForm").addEventListener("submit", async function (event) {
          event.preventDefault(); // Prevent the default form submission
      
          const form = document.getElementById("editBlogForm");
      
          // Create FormData object for form data, including the image file
          const formData = new FormData();
          formData.append("title", form.title.value);
          formData.append("content", form.content.value);
          formData.append("author_id", form.author_id.value);
      
          // Append the image file if one is selected
          if (form.imageUrl.files[0]) {
            formData.append("imageUrl", form.imageUrl.files[0]);
          }
      
          const blogId = "<%= blog.blog_id %>"; // Assuming `blog_id` is the identifier in your database
      
          try {
            const response = await fetch(`/api/admin/blogs/${blogId}`, {
              method: "PUT",
              body: formData, // Send FormData directly without setting Content-Type
            });
      
            if (response.ok) {
              alert("Blog updated successfully");
              window.location.href = "/api/admin/blogs"; // Redirect to blog list
            } else {
              const errorData = await response.json();
              const errorMessage = Array.isArray(errorData)
                ? errorData.join(", ")
                : errorData.message || JSON.stringify(errorData);
              alert("Failed to update blog: " + errorMessage);
            }
          } catch (error) {
            console.error("Error:", error);
            alert("An error occurred while updating the blog");
          }
        });
      </script>      
  </body>
</html>
